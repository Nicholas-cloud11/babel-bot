# Babel Bot

This bot will store user information (and hopefully store OC info eventually)

## Getting Started

These instructions will help you set up the project on your local machine for development.

### Prerequisites

- Python 3.8 or higher
- Git

### Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   cd YOUR_REPO_NAME
   ```

2. **Create a virtual environment**
   ```bash
   # On Windows
   python -m venv venv
   venv\Scripts\activate

   # On macOS/Linux
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **Install dependencies**
   ```bash
   pip install -r requirements.txt
   ```

### Running the Project

```bash
# Add instructions on how to run your code
python main.py
```

## Project Structure

```
.
├── README.md
├── requirements.txt
├── .gitignore
└── your_code_files.py
```

## Contributing

1. Create a new branch for your feature (`git checkout -b feature/amazing-feature`)
2. Make your changes
3. Commit your changes (`git commit -m 'Add some amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

## Team Setup

When joining this project:
1. Follow the installation steps above
2. Make sure you're working in the virtual environment (you should see `(venv)` in your terminal)
3. Before starting work, pull the latest changes: `git pull origin main`
4. Create a new branch for your work

## License

Add your license information here
